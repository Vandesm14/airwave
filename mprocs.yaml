procs:
  server:
    cmd:
      - cargo
      - run
      - --bin
      - airwave
      - --release
      - --
      - --logs-path
      - logs
  client-watch:
    cmd: ['pnpm', 'client-web:build', '--watch']

hide_keymap_window: true
