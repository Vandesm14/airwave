procs:
  server:
    cmd:
      - cargo
      - run
      - --bin
      - airwave
      - --release
      - --
      - --logs-path
      - logs
      # - --no-api
  client-watch:
    cmd: ['pnpm', 'client-web:build', '--watch']
